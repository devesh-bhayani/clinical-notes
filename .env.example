# === Data Paths ===
MIMIC_DATA_DIR=          # Path to MIMIC discharge notes (HIPAA-regulated, never commit)
DRUGBANK_VOCAB_PATH=     # Path to drugbank_vocabulary.csv
SPLITS_DIR=              # Path to data/splits/ for train/val/test JSONL

# === Model ===
BASE_MODEL_NAME=BioMistral/BioMistral-7B
CHECKPOINT_DIR=          # Where to save/load model checkpoints

# === API Keys ===
ANTHROPIC_API_KEY=       # For rejection generation via Claude API
WANDB_API_KEY=           # Weights & Biases tracking

# === Server ===
API_PORT=8000
