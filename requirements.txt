# Core ML/training stack. Validated together on Python 3.13 + CUDA (torch 2.x).
# The trainer uses TRL's `processing_class` API (trl >= 0.12) and includes a
# shim for transformers 5.x (see train/orpo_train.py); trl 0.9.x will NOT work.
transformers>=4.46.0
peft>=0.12.0
trl>=0.12.0
bitsandbytes>=0.43.3
datasets>=2.20.0
accelerate>=0.33.0
wandb>=0.17.0
pyyaml>=6.0
fastapi>=0.111.0
uvicorn>=0.30.0
streamlit>=1.36.0
bert-score>=0.3.13
python-dotenv>=1.0.0
httpx>=0.27.0
pytest>=8.2.0
spacy>=3.7.0
pandas>=2.2.0
rouge-score>=0.1.2
rapidfuzz>=3.9.0
